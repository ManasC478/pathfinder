import { FinishIcon } from "../../styles/icons";
import { Box } from "@chakra-ui/react";

const EndNode = () => {
  return (
    <span>
      <FinishIcon boxSize={5} />
    </span>
  );
};

export default EndNode;
